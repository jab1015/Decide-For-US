# App-specific R8/ProGuard rules for Decide For Us.
# Flutter, Firebase, RevenueCat, and other Android libraries provide their own
# consumer keep rules. Add only narrowly scoped rules here when a release build
# or runtime test proves one is required.
