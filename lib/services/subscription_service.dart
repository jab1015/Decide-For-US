class SubscriptionService {
  /// 🔥 TEMP: ALWAYS SUBSCRIBED
  static bool get isSubscribed => true;

  /// 🔥 KEEP THIS so your app doesn't crash
  static Future<void> init() async {
    // No-op for now (bypassed during testing)
  }
}