name: decide_for_us
description: A decision-making app

publish_to: 'none'

version: 1.0.15+20

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # Local storage
  shared_preferences: ^2.2.2

  # Subscriptions (RevenueCat)
  purchases_flutter: ^6.30.0

  # Firebase (compatible versions)
  firebase_core: ^2.30.0
  firebase_auth: ^4.16.0

  # ✅ FIXED (compatible + includes privacy manifest)
  share_plus: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true