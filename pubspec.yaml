name: decide_clean
description: Decide For Us App
publish_to: 'none'

version: 1.0.15+16

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  geolocator: ^10.1.0
  
  # UI / Core
  cupertino_icons: ^1.0.6
  confetti: ^0.7.0
  audioplayers: ^5.2.1

  # STORAGE
  shared_preferences: ^2.2.2

  # NETWORK
  http: ^0.13.6

  # FIREBASE (UPDATED — REQUIRED)
  firebase_core: ^3.6.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.4.0

  # SUBSCRIPTIONS (UPDATED — REQUIRED)
  purchases_flutter: ^9.15.1

  # SHARING
  share_plus: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/