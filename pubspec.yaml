name: decide_for_us
description: A decision-making app

publish_to: 'none'

version: 1.0.15+20

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.2

  # ✅ UPDATED (CRITICAL FIX)
  purchases_flutter: ^9.16.0

  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0

  # keep compatible version
  share_plus: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true