# Frontend directory

This directory contains an unused React/Vite scaffold. The production Decide
For Us client is the Flutter application in `lib/`.

Do not add V2 product work here unless a separate web product is intentionally
approved. If the scaffold remains unused when repository cleanup begins, remove
the directory rather than maintaining two frontend stacks.

